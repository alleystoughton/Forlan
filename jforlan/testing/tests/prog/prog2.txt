app(app(calc(isNil,
             calc(isInt,
                  calc(isNeg,
                       calc(isZero,
                            calc(isPos,
                                 const(nil)))))),
        calc(isSym,
             calc(isStr,
                  calc(isPair,
                       calc(isLam,
                            const(nil)))))),
    app(calc(plus,
             calc(minus,
                  calc(compare,
                       calc(fst,
                            calc(snd,
                                 const(nil)))))),
        calc(consSym,
             calc(deconsSym,
                  calc(symListToStr,
                       calc(strToSymList,
                            const(nil)))))))
